# CulinArch.AI — Environment Variables
# Copy this file to `.env.local` and fill in the values as needed.
# Never commit `.env.local` or any file containing real secrets.

# Public site URL (used for canonical links, SEO, OG tags)
# NEXT_PUBLIC_SITE_URL=https://culinarch.ai
